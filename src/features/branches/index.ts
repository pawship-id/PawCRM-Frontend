/**
 * Public surface of the branches feature (Master Data → Branch).
 *
 * Pages import from here, never from deep component paths. The three screen
 * entry points map onto the three routes: list, create, edit.
 */
export { BranchesScreen } from "./components/BranchesScreen";
export { BranchCreateForm } from "./components/BranchCreateForm";
export { BranchEditForm } from "./components/BranchEditForm";

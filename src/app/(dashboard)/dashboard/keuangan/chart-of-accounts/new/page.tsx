import { redirect } from "next/navigation";

/** Moved to Pengaturan — see ../page.tsx. */
export default function MovedNewChartOfAccountPage() {
  redirect("/dashboard/pengaturan/daftar-akun/new");
}

/**
 * Foundation placeholder.
 *
 * Intentionally minimal — this branch sets up the project skeleton only.
 * The real landing, authentication and dashboard routes arrive with their
 * own feature branches.
 */
export default function Home() {
  return (
    <main className="flex flex-1 items-center justify-center p-8">
      <div className="max-w-md text-center">
        <h1 className="text-2xl font-semibold tracking-tight">PawCRM</h1>
        <p className="mt-2 text-sm text-foreground/60">
          Frontend foundation is running.
        </p>
        <p className="mt-6 text-xs text-foreground/40">
          No features implemented yet.
        </p>
      </div>
    </main>
  );
}
